# Secrets — never commit
.env

# Runtime state — regenerated on each run
state/last-tokens.json
state/last-tokens.json.bak
state/*.log

# Python noise
__pycache__/
*.pyc
.venv/
venv/

# OS noise
.DS_Store
