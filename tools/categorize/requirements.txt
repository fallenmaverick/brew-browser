# brew-browser categorize tool
# Pick one of openai / anthropic — install whichever provider you use.
# Both can be installed; script auto-detects.

requests>=2.31,<3
python-dotenv>=1.0,<2

# Optional, one of:
openai>=1.50,<2
anthropic>=0.40,<1
